
name: Manual workflow
on:
  workflow_dispatch:

jobs:
  execBuild:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Worker
      env:
        TEST: ${{ secrets.TESTINGSECRET }}
      run: node index.mjs
